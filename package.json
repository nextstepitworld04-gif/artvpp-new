{
    "name": "artvpp-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build:frontend": "cd frontart && npm install && npm run build",
        "build:backend": "cd backend && npm install",
        "vercel-build": "npm run build:frontend && npm run build:backend"
    }
}